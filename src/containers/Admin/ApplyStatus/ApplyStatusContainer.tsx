import React from "react";
import { observer } from "mobx-react";
import ApplyStatus from "components/Admin/ApplyStatus";

const ApplyStatusContainer = ({}) => {
  return <ApplyStatus />;
};

export default observer(ApplyStatusContainer);
