import React from "react";
import WritePrintContainer from "containers/Write/WritePrint/WritePrintContainer";

const RegisterPage = () => {
  return <WritePrintContainer />;
};

export default RegisterPage;
